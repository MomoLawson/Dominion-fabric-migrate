package cn.lunadeer.dominion.utils;

/**
 * Enum representing the type of user interface preferred by the player.
 * Ported from the API PlayerDTO.UI_TYPE enum.
 */
public enum MessageDisplay {
    ;
    // Placeholder - actual MessageDisplay implementation will be ported later
    // when the full UI system is ported.
}
